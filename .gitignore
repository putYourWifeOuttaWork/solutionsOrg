# Swift Package Manager
.build/
.swiftpm/

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.xcuserstate

# XcodeGen-generated artifacts — project.yml is the single source of truth.
# Regenerate with `cd App && xcodegen generate`.
App/PrepOS.xcodeproj/
App/PrepOSApp.entitlements

# macOS
.DS_Store

# Secrets — NEVER commit. All secrets live in the Keychain (see CLAUDE.md §1).
*.env
.env*
secrets.json
*.p8
*.p12

# Local databases / exports
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.prepos
